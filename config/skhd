# Custom keyboard shortcuts to spawn apps
#
# Docs can be found here:
# https://github.com/koekeishiya/skhd

# open iterm2:
cmd + shift - return : open -b "com.googlecode.iterm2"

# open vscode:
ctrl - p : open -a 'GoLand'

# open browser:
ctrl - o : open -a 'Arc'

# open telegram:
ctrl - a : open -a 'Telegram'

# Hyper has its own hotkey with `ctrl + ;` in `hyper.js`,
# because this app does not allow `;` as a hotkey for some reason.
